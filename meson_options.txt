# SPDX-License-Identifier: MIT
# Copyright (c) 2026 Michael Rolnik <mrolnik@gmail.com>

option(
    'storage',
    type        : 'combo',
    choices     : ['lmdb', 'memory'],
    value       : 'lmdb',
    description : 'metadata storage backend (openPrfs). More engines add a choice here.',
)

option(
    'lua',
    type        : 'boolean',
    value       : true,
    description : 'build the sol2 Lua bindings (prfs-lua) and the prfs-test scenario runner.',
)

option(
    'content',
    type        : 'boolean',
    value       : true,
    description : 'build the content provider (libprfs-content), the READ-path byte generator.',
)

option(
    'rng',
    type        : 'combo',
    choices     : ['philox', 'threefry'],
    value       : 'philox',
    description : 'build-time default content RNG (run-wide; overridable once at startup).',
)

option(
    'plugins',
    type        : 'boolean',
    value       : true,
    description : 'build the plugin host (prfs-host, spdlog+CLI11) and the null test plugin.',
)
